export const GET_CRAFT_STATUS_DESCRIPTION = [
    'Get the craft processes on a cell: each one’s recipe, status (active / pending payment), batches done,',
    'what is claimable right now, and when the next batch matures. Public — works for any tokenId. This is the',
    'source of craft progress; bank matured batches with `claim_craft`.',
].join(' ');
