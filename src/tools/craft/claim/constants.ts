export const CLAIM_CRAFT_DESCRIPTION = [
    'Bank every matured craft batch on a cell you own into its resource balance. Requires a session — call',
    '`authenticate` first. Claiming with nothing matured is a no-op success; a fully-claimed process frees',
    'its slot. Check what is claimable first with `get_craft_status`.',
].join(' ');
