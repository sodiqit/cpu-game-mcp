export const GET_MINING_STATUS_DESCRIPTION = [
    "Read a cell's mining status: whether an extractor is active, which resource it mines, the",
    'accrued-but-unclaimed amount, and the remaining deposit. Public — works for any cell, no session',
    'required. The owner banks the accrued amount with `claim_mining`.',
].join(' ');
